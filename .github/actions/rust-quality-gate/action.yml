name: Rust Quality Gate
description: Ensure the Rust workspace is formatted and clippy-clean.

runs:
  using: composite
  steps:
    - name: Check formatting
      shell: bash
      run: cargo fmt --all -- --check

    - name: Clippy
      shell: bash
      run: cargo clippy --workspace --all-targets -- -D warnings
