[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "usfm3"
version = "0.1.6"
description = "Error-tolerant USFM 3.x parser"
readme = "README.md"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
